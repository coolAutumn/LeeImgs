由于这个工程本来就这开源的,为了能够更好地支持可扩展性,本模块面向接口编程.
    所有需要传输的对象需要实现ITransObject接口,用户自己定义的类只要实现ITransObject接口,也可以被OutPut类接受从而输出.

这个模块就像是一个管道一样,利用接口把每个对象的输出规范一下,这样到controller里的时候能够知晓类型并且只要根据类型做输出方式变化即可.
这个模块一般是在找到该有的信息后再利用这个模块的管道特性做个规范简化.

